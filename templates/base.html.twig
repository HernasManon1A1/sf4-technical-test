<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}- Test Technique SF4{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('build/main.css') }}">
            <link rel="stylesheet" href="{{ asset('build/snippet.css') }}">
        {% endblock %}
    </head>
    <body>
        {% if app.user %}
            <nav class="navbar navbar-default" role="navigation">
                <div class="container-fluid">
                    <ul class="nav navbar-nav">
                        <li class=""><a href="{{ path('home') }}">Technical Test Symfony4</a></li>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">
                        <li class="nav-divider"></li>
                        <li class="navbar-text">Bonjour {{ app.user.username|capitalize}}</li>
                        <li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-log-out"></span> Déconnexion</a></li>
                    </ul>
                </div>
            </nav>
        {% endif %}
        <div class="container">
            {{ include('partials/flashbags.html.twig') }}
            {% block body %}{% endblock %}
            {% block javascripts %}
                <script src="{{ asset('build/main.js') }}"></script>
                <script src="{{ asset('build/snippet.js') }}"></script>
            {% endblock %}
            {{ include('partials/particles.html.twig') }}
        </div>
    </body>
</html>
